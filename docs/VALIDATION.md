# 内部试用版验证记录

日期：2026-09-14。机器：macOS 14.8.4（23J319）、Apple Silicon arm64。工具链：Node.js 24.10.0、npm 11.6.0、Rust 1.91.0、系统 Command Line Tools。

## 自动化验证

- `npm run build`：TypeScript 与 Vite 生产前端构建通过。
- `npm run check`：TypeScript + Cargo Clippy 全目标，`-D warnings`，通过。
- `npm test`：15 项 Rust 集成测试通过，0 失败。
- `npm run tauri build -- --bundles app`：生成原生 Release `.app`。
- `codesign --verify --deep --strict --verbose=2`：本地完整包 ad hoc 签名校验通过，valid on disk。
- 最终 Release 的隔离 QA 副本通过 LaunchServices 启动；读取原有 3 个服务、13 个配置目标和待应用状态。QA 副本仅修改应用标识及测试目录环境变量，交付包未带入这些测试环境变量。

测试覆盖：

1. 12 个适配器的能力范围、格式往返、其他配置与注释保留。
2. 来源专属原生字段保留、不支持结构拒绝接管。
3. 基于各工具官方示例的 HTTP 输出独立断言，包括 Cline / Roo 不同类型字段。
4. 精确恢复原始文件、重开服务库仍保持正确基线。
5. 预览后外部修改会阻止写入。
6. 已有配置纳入管理不产生写入；外部冲突的显式处理。
7. 未管理同名条目不会被静默覆盖。
8. 整批导入原子性、移除与撤销。
9. 中断日志自动恢复；外部编辑保留并进入待处理状态。
10. 恢复状态落盘与历史落盘之间再次中断，重启后仍阻止直接写入。
11. 保留当前磁盘文件可解除恢复阻塞，并能持久化。
12. 后来修改的文件不被历史恢复覆盖；同一工作区进程锁。
13. 脱敏保留数组等必要结构。
14. 路径别名 / 数据目录冲突、重复 JSON 属性名阻止歧义写入。
15. 第二个文件写入失败时，前一个文件恢复。

部分测试函数同时覆盖多个场景；以上为验收点，不是测试函数逐一对应列表。测试仅在项目临时目录运行。

## 真实桌面操作

通过 macOS 原生应用界面验收，Tauri IPC 连接 Rust 后端，未使用浏览器 mock。

| 流程 | 观察到的结果 |
| --- | --- |
| 空工作区首次启动 | 展示 12 种工具、5 个隔离样例配置位置、服务库为空 |
| 发现 Codex 配置 | context7 / filesystem 可选择接管 |
| 接管两个已有服务 | 库中出现 2 个服务，待应用为 0，原文件不改 |
| Filesystem 分配给 Cursor | 产生 1 项待应用；差异默认隐藏命令和参数 |
| 完整差异切换 | 显示真实独立参数数组，预览生成正确的 Cursor JSON |
| 应用变更 | UI 显示配置已对齐；磁盘回读包含新增 filesystem，原 context7 保留 |
| 恢复原文件 | Cursor 文件回读恢复仅有 context7；服务库仍保留待应用意图 |
| 编辑服务 | 中文显示名称及包含空格的参数完整保存；配置键保持不变 |
| 导出 | 原生保存对话框将脱敏 JSON 写入隔离目录；文件回读确认命令和参数已隐藏 |
| 配置检查 | 检出本机 npx 位置；界面明确提示未启动服务或联网 |
| 退出并重启 | 服务、名称、分配和待应用状态均保留 |
| 添加目标 | 同一 Codex 适配器新增项目路径，显示第 13 个独立配置目标 |
| 文本导入 HTTP | 新服务进入库且未自动分配 |
| Cmd+K 搜索 | 聚焦搜索并正确过滤服务、切换详情 |

验收使用 `.local-dev/fixture-home`、`.local-dev/fixture-data`、`.local-dev/sample-project`，没有测试写入真实 Agent 配置，也没有启动样例 MCP 命令。

## 没有验证或不在本版范围

- 未在 12 个第三方客户端分别完成 MCP 握手、工具调用、OAuth 与组织策略测试。不能将适配器格式测试表述为真实客户端全兼容认证。
- 未验证 Intel Mac、Windows、Linux 或 macOS 12 实机；配置最低系统版本不等于所有最低版本都已实测。
- 未完成 Developer ID 签名、Apple 公证、安装器或自动更新。
- 未接入钥匙串、加密凭据存储、跨设备同步、远程服务健康探测。
- 多文件操作通过备份日志、校验和补偿恢复实现，不具备操作系统级跨文件原子事务。

本轮 UI 验证没有对用户现有 Agent 配置进行写入。调试样例、依赖缓存、构建产物都被 Git 忽略。

## 选择框与弹出菜单更新（2026-09-14）

使用统一的 `Select.tsx` 组件替换五处原生选择框：发现目标、来源格式、导出格式、传输方式、目标适配器。选择框保持统一高度、边框和焦点样式；弹出菜单采用浅色圆角面板，包含工具图标、名称、配置数量、悬停高亮及选中标记。长列表可滚动，菜单位于原生 dialog 的同一顶层中，并按可用空间定位。

TypeScript / Vite 和 Tauri Release 构建通过，应用签名完整性检查通过。在原生 Tauri 窗口完成以下验证：

- 菜单样式及选中项显示正常，滚动区域包含所有配置目标。
- 鼠标选择 Claude Code 后，触发框、配置路径、数量和发现列表同步更新。
- 点击菜单外可以关闭选项列表。
- Tab 可以聚焦选择框，方向键展开和切换高亮，Enter 确认选择。
- End 自动滚动到最后一个目标。
- Escape 只关闭菜单，保留外层发现弹窗，原选择值不改变。
- 鼠标打开后明确保留组合框焦点，兼容 WebKit 的按钮焦点行为。

实现改动限定在 `src/Select.tsx`、相关表单调用和下拉样式中，Rust 配置引擎没有改动。本轮未重复运行 Rust 测试。

## 滚动稳定性修复（2026-09-14）

为页面、侧栏、服务列表、详情、弹窗、代码和文本输入区、选项菜单统一设置 `overscroll-behavior: none`，关闭边界回弹及滚动向外层传递。菜单鼠标悬停改为 CSS 高亮，不改变键盘活动项，也不触发自动滚动。只有打开菜单或键盘导航时才确保活动选项可见；手动滚动后再次按 Home / End，也能定位到对应项目。

验证：TypeScript / Vite 与 Tauri Release 构建通过。原生窗口中连续滚动到菜单底部，再继续向下滚动，列表没有跳回已选项，菜单和外层弹窗边框位置保持一致；手动滚动后按 Home 可以返回首项。配置引擎与数据结构未改动，未重复运行 Rust 测试。

## macOS 图标视觉尺寸修复（2026-09-14）

增加 macOS 图标的透明留白，512 px 图标的可见边界从 `(16,16)–(496,496)` 调整为 `(48,48)–(464,464)`，主体宽高缩小约 13.3%。图案、配色和中心位置保留；新增 `src-tauri/icons/macos-icon.svg` 作为可重新生成的 macOS 源文件，其他平台图标不变。

验证：检查 ICNS 内全部 10 个尺寸表示，确认透明边距和居中；重新运行 Tauri bundle，正式包与 QA 包引用的 ICNS 和源资源逐字节一致，两者完整包签名校验通过。QA 应用重新启动成功，仍使用隔离测试目录。此次仅调整图标资源及文档，未重新编译业务代码或重复运行 Rust 测试。

## 导入与导出图标修正（2026-09-14）

导入配置入口和选择配置文件使用向下进入的箭头，导出服务库和保存导出文件使用向上离开的箭头，统一四处图标的方向语义。改动仅涉及 `src/App.tsx` 中的图标引用，导入、导出行为保持一致。TypeScript / Vite 和 Tauri Release 构建通过。

## 发现列表状态与全选（2026-09-14）

不支持项采用浅琥珀色背景、警示图标、左侧强调线和“不支持”标签；已管理项使用中性背景；可导入项提供选中反馈。增加三态“全选可导入项”和已选数量，提交前过滤不可导入与已管理条目。

TypeScript / Vite、Tauri Release 构建以及安装包签名校验通过。使用独立 UI 样例工作区验证：2 条可导入、1 条不支持、1 条已管理的列表全选后仅选中 2 条；取消单条呈现原生中间态；部分选中再点击全选会补全，再次点击清空；切换目标清空选择；全选纳入后服务数从 1 变为 3，未重复导入或纳入不支持项，源配置逐字节不变；重新发现时全选与提交按钮均禁用。已安装应用更新后，在真实 Cursor 列表确认状态样式及全选入口显示正常。

Rust 配置引擎未改动，本轮未重复运行核心测试。批量纳入验证使用单独的项目内样例，真实配置仅用于查看界面。

## 目标工具品牌图标（2026-09-14）

12 种适配器统一使用对应应用包或官方站点、仓库中的原始图标，资源位于 `public/tool-icons/`，来源、版本及 SHA-256 可追溯。共享 `ToolIcon` 组件覆盖侧栏、服务分配、欢迎页、目标管理和各选择菜单；图标保持原配色与比例，离线加载。

验证：TypeScript / Vite 与 Tauri Release 构建通过，12 项映射与资源文件逐一对应，所有文件 SHA-256 与获取时记录一致，SVG 不含脚本或外部链接内容。原生安装版确认侧栏、服务分配与 16 px 服务列表图标显示正常；独立图标 QA 工作区确认欢迎页全部 12 个图标、下拉菜单上下两段以及 End / Enter 选中 Claude Desktop 后触发框图标和名称一致。正式安装包签名校验通过，未携带 QA 环境变量。

改动仅涉及图标组件、样式、资源和文档。未重复运行 Rust 核心测试；图标 QA 未纳入服务、应用同步或启动 MCP 进程。

## 目标栏仅显示有服务的工具（2026-09-14）

左侧目标栏按服务库中未删除、已分配的服务计数，仅展示数量大于 0 的目标。刷新快照后，若当前工具已无服务，则自动切回“全部服务”。目标管理、发现配置及服务分配继续提供完整目标列表。

TypeScript / Vite、Tauri Release 构建和安装包签名校验通过。隔离原生 QA 验证：空库时侧栏无工具；纳入两条 Codex 服务后只显示 Codex 2；选中 Codex 并取消一条分配后数量为 1；移除最后一条后 Codex 隐藏、自动选中“全部服务”，仍能看到未分配服务；目标管理保持 12 个目标。测试目录内 5 份源配置的 SHA-256 均保持不变，未应用同步或启动 MCP。

安装版确认只显示 Codex 2 和 Cursor 8，原有 10 个服务、1 项待应用及修订 9 保留。此次仅修改 `src/App.tsx` 和验证记录，未重复运行 Rust 核心测试。

## Codex 图标与可见尺寸校准（2026-09-14）

Codex 保留蓝色云朵，资源从带不透明白色方底的 ICNS 表示改为应用包内原始 `icon-codex-light.png`，保留官方圆角浅色底座及透明外沿。Claude Code 与 Claude Desktop 共用同一个资源和显示参数。Codex、Cursor、Claude 与 VS Code 的原始 Dock 边距通过 SVG 显示视口校准，主体按统一尺寸显示，原始 PNG 未重绘或缩放。

验证：TypeScript / Vite、Tauri Release 与签名完整性检查通过。12 种工具映射到 11 份资源，SHA-256 与来源一致；Codex 四角透明；4 份带 Dock 边距的资源，其显示边界均与 alpha > 240 的主体范围一致。隔离原生窗口检查欢迎页、下拉菜单上下两段及 Claude 两端图标一致性；正式安装版检查 26 px 侧栏和分配列表、16 px 服务列表。24 px 菜单中的图标正常加载，Codex 无原先的白色直角方块。

此次仅修改共享图标组件、样式、资源与文档。QA 未纳入服务、应用同步或启动 MCP，未重复运行 Rust 核心测试。

## 弹窗滚动条圆角边界（2026-09-14）

滚动区域从整个 `dialog` 移至 `.modal-body`，外层裁切圆角，打开的弹窗使用纵向 flex 布局，标题栏与底部按钮不收缩。内容区设置 `min-height: 0` 和独立纵向滚动，并保留禁止边界回弹的行为。

TypeScript / Vite、Tauri Release 构建及正式包签名校验通过。隔离原生窗口验证“工具与配置路径”12 项长列表：顶部滚动条从标题分隔线下方开始，滚动到底部后在底栏上方结束，四角没有滚动条溢出，标题及底栏位置保持一致，最后一项与数据目录可见，“完成”按钮可正常关闭弹窗。“发现本机配置”的目标下拉菜单仍可完整展开，未受内容区裁切影响。

已更新正式安装版。改动仅涉及共享弹窗 CSS 与验证记录；QA 只查看与滚动，没有写入 Agent 配置，未重复运行 Rust 核心测试。

## 应用图标定稿与菜单滚动边界（2026-09-14）

应用图标采用用户最终选定的 A 方案：放大的叠放卡片、插头符号、哑光绿底。`src-tauri/icons/app-icon-source.png` 保存原始透明 PNG，SHA-256 为 `1ca565aacf7e37bee394bdc1e620bff5aa7d255d9e808119d1215cebc46438c8`，与选定图片逐字节一致。通过 Tauri CLI 生成打包图标，侧栏、启动页与 favicon 使用同一图案，移除旧图标源文件。

下拉菜单改为外层圆角容器与内层滚动列表，滚动条上下端收进 5 px 内边距；键盘定位使用内层列表，点击外部关闭的判定仍覆盖整个菜单。

验证：

- TypeScript / Vite 与 Tauri Release 构建通过，正式包及隔离 QA 包完整签名校验通过。
- ICNS 中 10 个尺寸表示的四角均透明；256 px 图标主体边界为 `(22,22)–(234,234)`，512 px 为 `(44,44)–(468,468)`。原生应用侧栏中图案正常显示。
- 隔离原生窗口检查目标菜单顶部、滚动到底部的样式：滚动条均位于面板内边距中，圆角完整；最后一项 Claude Desktop 可见。Home / End / Enter 导航后正确选择 Claude Desktop，触发框、路径与空配置提示同步更新。
- 已替换 `/Applications/MCP Deck.app` 并重新启动；安装包图标与仓库 ICNS 逐字节一致，应用标识仍为 `com.mcpdeck.desktop`，没有 QA 环境变量。安装前后界面均为 10 条服务、1 项待应用、修订 9。
- QA 的 5 份源配置指纹保持不变；本轮没有纳入服务、应用同步或启动 MCP，也没有改动 Rust 配置引擎或重复运行其测试。

## 官网工具图标与 SVG 优先（2026-09-14）

Codex、Claude、Cursor、VS Code 与 Cline 改用官网资源。Cursor、VS Code、Cline 使用官方品牌包中的独立 SVG，Claude Code 与 Claude Desktop 共用官网字标中的橙色星形 SVG 路径。Codex 使用官网当前展示的 ChatGPT / OpenAI Blossom PNG，保留透明通道，并按原图画布校准显示。具体来源、导出方式和 SHA-256 记录在 `public/tool-icons/README.md` 与 `sources.json`。

验证：TypeScript / Vite 与 Tauri Release 构建通过；全部 11 份资源的 SHA-256 与来源记录一致，组件引用均有对应文件，SVG 不包含脚本、事件处理器或外部资源引用。原生隔离 QA 欢迎页确认 12 个工具图标正常显示，Claude 两端图案一致，Cline 机器人、Cursor 立方体和 VS Code 蓝色标识可辨认。正式安装版确认侧栏 26 px、服务列表 16 px 和分配列表中的新资源已加载，签名校验通过，未携带 QA 环境变量。

此次改动限于共享图标映射、资源与来源文档。未操作服务分配、写入 Agent 配置或启动 MCP；未重复运行 Rust 核心测试。安装前后均为 10 条服务、1 项待应用、修订 9。

## Gemini CLI、OpenCode 与 Roo Code SVG（2026-09-14）

Gemini CLI 改用官方品牌页的彩色终端 SVG，Roo Code 改用官方扩展仓库的袋鼠 SVG，两者与下载文件逐字节一致。OpenCode 使用官方品牌页提供的浅色背景方形 SVG，仅解码其下载资源的 data URI，保留原始路径、颜色与视口。Codex 按用户选择保留上一版官网 PNG，资源字节与原版本一致。

验证：TypeScript / Vite 与 Tauri Release 构建通过；全部 11 份图标资源的 SHA-256 与来源记录一致，SVG 无脚本、事件处理器或远程资源。隔离原生 QA 欢迎页确认全部 12 个工具正常显示，Codex 已恢复 PNG，Gemini CLI、OpenCode 与 Roo Code SVG 图案清晰。正式安装版确认侧栏 Codex 和分配列表中的 Codex、Gemini CLI、OpenCode 正常加载；安装包与构建产物的可执行文件一致，完整签名校验通过，未携带 QA 环境变量。安装前后均为 10 条服务、0 项待应用、修订 10。

改动仅涉及三个工具的资源映射、图标文件和来源文档。未操作服务分配、应用同步或启动 MCP，也未重复运行 Rust 核心测试。

## Codex 官方花结 SVG（2026-09-14）

Codex 改用 OpenAI 官方品牌包 `openai-logos.zip` 内的 `OpenAI-logos/SVGs/OAI_OpenAI-Blossom_Black.svg`，其花结主体与先前 Codex 官网 PNG 对应。使用未经改写的原始 SVG，显示层将 716 px 画布的留白校准为 `178 178 360 360` 视口。原始路径、颜色和比例保持不变。

验证：全部 11 份资源均为 SVG，SHA-256 与来源记录一致，Codex 与品牌包条目逐字节一致；SVG 无脚本、事件处理器或远程资源。TypeScript / Vite、Tauri Release 构建及正式包、隔离 QA 包签名检查通过。原生 QA 欢迎页确认 12 个工具图标正常显示；正式安装版的 Codex 侧栏和分配列表显示黑色花结，无圆形终端符号及 PNG 底座，尺寸与其他工具协调。安装包可执行文件与构建产物一致，未携带 QA 环境变量。安装前后均为 10 条服务、0 项待应用、修订 10。

仅更新 Codex 图标映射、资源与来源文档；未改动配置引擎、应用同步或启动 MCP，未重复运行 Rust 核心测试。

## 同步预览完整差异切换闪烁（2026-09-14）

“显示完整差异”原先复用全局 `busy`，每次请求会同时切换背景刷新按钮、分配控件的禁用样式及底部状态文字。改用独立的 `previewLoading`，请求期间保留已显示的差异，成功后同时更新差异和勾选状态；失败后保留原内容，重试时清除旧错误。预览加载期间继续阻止应用、冲突处理和关闭弹窗。

验证：使用仅含合成配置的临时浏览器 QA 页面，为预览请求加入 1.5 秒延迟并观察背景 DOM。同一次完整差异切换，旧版背景节点变化为 10 次，修复版为 0 次；两版弹窗节点均保持同一实例。修复版完整/脱敏双向切换均通过，加载时原差异保留、背景刷新图标及状态栏不变、应用按钮禁用。模拟请求失败后保持脱敏视图和未勾选状态，重试成功、旧错误消失；请求中点击“返回”没有提前关闭弹窗。QA 仅允许读取合成快照和预览，未使用真实凭据。

TypeScript / Vite 与 Tauri Release 构建通过。隔离原生 QA 预览弹窗正常打开；正式 App 已替换并启动，签名、可执行文件一致性与无 QA 环境变量检查通过。安装前后均为 10 条服务、0 项待应用、修订 10。仅修改 `src/App.tsx` 和本验证记录，未改动配置引擎，未应用同步或启动 MCP，未重复运行 Rust 核心测试。

## 完整差异显示与同步计划解耦（2026-09-14）

上述修复只隔离了全局加载状态，切换完整差异仍会重读配置、替换内存中的同步计划，并更新应用按钮的加载状态。现改为打开弹窗时由同一次预览生成完整与脱敏两份内容，默认展示脱敏内容；勾选框只切换本地显示，不调用预览接口、不替换计划 ID，也不修改按钮样式或加载状态。处理冲突后刷新弹窗时重新生成两份内容。默认概览接口仍仅返回脱敏差异；实际应用前继续检查工作区修订及磁盘原文。

验证：

- TypeScript / Vite、Tauri Release 构建及 `cargo clippy --all-targets -- -D warnings` 通过。
- 18 项核心测试通过，新增测试覆盖同一计划的完整／脱敏内容、默认接口不返回完整内容、按计划写入正确配置、零变更、冲突保留，以及外部编辑后在任何写入前拒绝应用。
- 合成浏览器 QA 验证完整／脱敏双向切换、重新打开默认脱敏及零变更场景。每次切换观察 2 秒，未新增预览请求，底栏 DOM 变化为 0；按钮颜色、透明度和可用状态均保持不变。
- 隔离 Tauri 原生 QA 验证 1 项变更的连续切换与 0 项变更的勾选。完整、脱敏和再次脱敏截图中的应用按钮区域像素一致；零变更切换前后按钮区域像素一致。截图比较针对采集到的状态，不等同于逐帧录像。合成配置文件内容保持不变，界面 QA 未执行应用或启动 MCP。
- 正式 App 已替换至 `/Applications/MCP Deck.app` 并启动，签名、可执行文件一致性、无 QA 环境变量检查通过。安装前后保留 10 条服务、1 项待应用及修订 13。

## Cursor 到 Codex 的具名 TOML 段落（2026-09-14）

用户反馈新增 `git` 项看起来缺少名称。原输出 `[mcp_servers]` 下的 `git = { ... }` 是合法的具名内联表；本机 Codex CLI 能识别 `git`，命令与参数也完整。为使新增项与 Codex 常见配置写法一致，写入器现在将本次新增／更新的服务输出为显式的 `[mcp_servers.git]` 段落；已有内联根表按需展开，带点或空格的服务名称正确引用，其他服务与非 MCP 设置保持原值。

验证：20 项核心测试、Clippy、TypeScript / Vite 和 Tauri Release 构建通过。新增用例先在旧代码上复现内联输出，再验证 Cursor `git` 导入、分配 Codex、预览、隔离目录应用、回读与再次预览无差异；已有 `node_repl` 原文及其 `startup_timeout_sec = 120` 保留。另覆盖已有内联条目的更新、内联 MCP 根表和特殊名称。

真实环境只读取配置并检查加载情况：Codex CLI 列出已启用的 `git`；当前终端环境中 `python3 -m mcp_server_git` 初始化成功并返回 12 个工具，未调用 Git 工具操作。这不等同于确认桌面会话已重新加载该配置；尚未复现用户所说的连接失败。服务库当前仅将 `git` 分配给 Cursor，已有 Codex 同步基线仍在，因此 1 项待应用为移除 Codex 的 `git`；未更改该选择。

正式 App 已更新并启动。签名、二进制一致性及无 QA 环境变量检查通过；安装前后 Cursor、Codex 配置和服务库内容指纹一致，界面保留 10 条服务、1 项待应用与修订 13。现有配置不会仅因升级自动重新排版；新的段落写法在后续服务新增或更新时生效。
